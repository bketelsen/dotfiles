#!/bin/sh

if [ ! -f "${HOME}/bjk/.config/chezmoi/key.txt" ]; then
    mkdir -p "${HOME}/bjk/.config/chezmoi"
    chezmoi age decrypt --output "${HOME}/bjk/.config/chezmoi/key.txt" --passphrase "{{ .chezmoi.sourceDir }}/key.txt.age"
    chmod 600 "${HOME}/bjk/.config/chezmoi/key.txt"
fi
